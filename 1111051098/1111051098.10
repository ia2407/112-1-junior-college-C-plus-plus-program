﻿// ConsoleApplication1.cpp : 此檔案包含 'main' 函式。程式會於該處開始執行及結束執行。
//

#include <stdio.h>

int main()
{

	int a, b, c, d, e; 
	scanf_s;
printf("輸入車速,內側有無車輛(0=無,1=有),相對距離,對方速度");
scanf_s("%d,%d,%d,%d", & a, &b, & c, & d);
If (b <= 0)
{
	printf("盡快超車");
}
Else
{e = a - d;
If (c > 30 && e < 30)
{
	printf("盡快超車");
}
Else
{
	printf("維持車道");
}

}

}